#!/usr/bin/python3
# Python function the same as the given Python bytecode


def magic_calculation(a, b, c):
    """function the same as the given Python bytecode"""
    if a < b:
        return (c)
    if c > b:
        return (a + b)
    return (a*b - c)
